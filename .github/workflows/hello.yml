name: hello

on:
  push:
    branches: [main]

jobs:
  build :
    runs-on: ubuntu-latest

    steps: 

      - name: checkout
        uses: actions/checkout@v3 
      - shell: |
          ls
      
      - name: hello
        run: echo 'hello vikas' 
      

